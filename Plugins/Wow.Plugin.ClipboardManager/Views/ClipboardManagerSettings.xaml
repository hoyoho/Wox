<UserControl x:Class="Wow.Plugin.ClipboardManager.Views.ClipboardManagerSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="560" d:DesignWidth="860">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12"
                   Foreground="Gray"
                   Text="{DynamicResource wox_plugin_clipboardmanager_settings_description}" />

        <!-- 条目表格:名称 | 内容 -->
        <Border Grid.Row="1" BorderBrush="#D0D0D0" BorderThickness="1">
            <DataGrid x:Name="ItemsGrid"
                      ItemsSource="{Binding Items}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="False"
                      CanUserResizeColumns="False"
                      IsReadOnly="False"
                      SelectionMode="Single"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto"
                      PreviewKeyDown="ItemsGrid_PreviewKeyDown"
                      CellEditEnding="ItemsGrid_CellEditEnding">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{DynamicResource wox_plugin_clipboardmanager_column_name}"
                                        Width="150"
                                        CanUserResize="False"
                                        Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="HorizontalAlignment" Value="Left" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Header="{DynamicResource wox_plugin_clipboardmanager_column_content}"
                                            Width="*"
                                            CanUserResize="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Text}"
                                           TextWrapping="Wrap"
                                           MaxHeight="44"
                                           VerticalAlignment="Center">
                                    <TextBlock.ToolTip>
                                        <TextBlock MaxWidth="700" MaxHeight="400" TextWrapping="Wrap"
                                                   Text="{Binding Text}" />
                                    </TextBlock.ToolTip>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         VerticalScrollBarVisibility="Auto"
                                         MinHeight="60" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- 底部:添加 / 删除所选 / 清空全部 -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="{DynamicResource wox_plugin_clipboardmanager_add}"
                    Padding="10,4"
                    Margin="0,0,10,0"
                    Click="AddButton_Click" />
            <Button Content="{DynamicResource wox_plugin_clipboardmanager_delete}"
                    Padding="10,4"
                    Margin="0,0,10,0"
                    Click="DeleteSelectedButton_Click" />
            <Button Padding="10,4"
                    Click="ClearAllButton_Click"
                    Content="{DynamicResource wox_plugin_clipboardmanager_clear_all}" />
        </StackPanel>
    </Grid>
</UserControl>
